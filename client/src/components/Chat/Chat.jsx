import { Box } from '@mui/material';
import { GroupMembers } from '../subcomponents/GroupMembers/GroupMembers';

export const Chat = () => {
  return (
    <Box mt={2}>
      <GroupMembers />
    </Box>
  );
};

export default Chat;
